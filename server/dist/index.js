import app from "./app.js";
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
    // consoleole.log(`Server is running on http://localhost:${PORT}`);
});
// // consoleole.log('index.ts')
